function setup() {
  createCanvas(4000, 4000);
}

function draw() {
  
  noStroke();
  fill(255,204,0)
  
  var size = random(1, 10); 
  
  if (mouseIsPressed)
    ellipse(mouseX,mouseY, size);

 if (keyIsPressed === true) {
    fill(0);
  } else {
    fill(255);
  }
  rect(25, 25, 50, 50);
}
